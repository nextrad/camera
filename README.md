# NeXtRAD Cameras    
    
    usage: camera.py
    
    view camera feeds from antennas
    
    optional arguments:
      -h, --help            show this help message and exit
      -r RESOLUTION, --resolution RESOLUTION
                            video resolution (low, med, high) [medium]